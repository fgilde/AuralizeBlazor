@page "/auto-gradients"

<PageTitle>Auto Gradients</PageTitle>

<DocsPageSection>
    <SectionHeader Title="Auto Gradients">
        <Description>
            With the Property <CodeInline>AutoApplyGradient</CodeInline> you can automatically apply gradients.
            You can use gradients from your background image, the cover image or if you use a tracklist from the tack image.
        </Description>
    </SectionHeader>
    <SectionContent Outlined="true" FullWidth="true">
        <AutoGradientsExample/>
    </SectionContent>
    <SectionSource Code="@nameof(AutoGradientsExample)"/>
</DocsPageSection>