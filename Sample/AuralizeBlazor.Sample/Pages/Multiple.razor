@page "/multiple"

<PageTitle>Multiple</PageTitle>


<DocsPageSection>
    <SectionHeader Title="Multiple">
        <Description>
            If you want to have multiple instances of the Auralizer connected to the same source, you need to set the <code>ConnectionMode</code> to <code>Stream</code> then you can use the <code>Auralizer</code> component multiple times.
        </Description>
    </SectionHeader>
    <SectionContent Outlined="true" FullWidth="true">
        <MultipleExample />
    </SectionContent>
    <SectionSource Code="@nameof(MultipleExample)" />
</DocsPageSection>